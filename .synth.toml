project = "test"

[settings]
auto_approve_tools = [
    "synth-mcp/send_message",
    "synth-mcp/check_delivery",
    "synth-mcp/launch_agent",
    "synth-mcp/list_agents",
]

[[agents]]
agent_id = "kiro"
harness = "kiro"
agent_mode = "coder"

[[agents]]
agent_id = "kiro-2"
harness = "kiro"
agent_mode = "coder"
